<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="shortMovieInfo"
            type="com.datafrey.movies.data.ShortMovieInfo" />

        <variable
            name="movieItemEventListener"
            type="com.datafrey.movies.main.MovieItemEventListener" />

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardCornerRadius="8dp"
        android:onClick="@{() -> movieItemEventListener.onClick(shortMovieInfo)}">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp">

            <ImageView
                android:id="@+id/posterImageView"
                android:layout_width="100dp"
                android:layout_height="150dp"
                app:url="@{shortMovieInfo.Poster}"
                android:layout_alignParentStart="true"/>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="4dp"
                android:layout_toEndOf="@+id/posterImageView"
                android:layout_centerVertical="true">

                <TextView
                    android:id="@+id/titleTextView"
                    android:text="@{shortMovieInfo.Title}"
                    android:textColor="@android:color/black"
                    android:textSize="20sp"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content" />

                <TextView
                    android:id="@+id/yearTextView"
                    android:text="@{shortMovieInfo.Year}"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>

            </LinearLayout>

        </RelativeLayout>

    </androidx.cardview.widget.CardView>

</layout>
